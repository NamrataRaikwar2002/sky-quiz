export type LoginUserInfo = {
    email:string,
    password: string
}

export type InputType = {
  passwordType:string,
  confirmpaswd:string
}

export type SignupUserInfo = {
  firstName: string,
    lastName: string,
    email: string,
    password: string,
    confirmPassword: string
}